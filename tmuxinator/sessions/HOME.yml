session_name: HOME
windows:
- window_name: journal
  layout: even-horizontal
  panes:
    - vim ~/notes/dailylog/$(date +%Y-%m).md -c "Goyo"
    - vit
- window_name: dotfiles
  layout: main-horizontal
  shell_command_before:
    - cd ~/dotfiles
  panes:
    - vim -c "NERDTree"
